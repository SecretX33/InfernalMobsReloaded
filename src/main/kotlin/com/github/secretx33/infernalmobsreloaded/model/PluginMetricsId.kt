package com.github.secretx33.infernalmobsreloaded.model

/**
 * Wrapper class for plugin metrics id because `Toothpick` does not support injection of primitive types.
 */
data class PluginMetricsId(val value: Int)
